package com.edu;

import java.util.Scanner;

//public class Arrays {

	//public static void main(String[] args) {
		//Scanner sc = new Scanner(System.in);
		/* for(int i = 0; i <numeros.length; i++) {
			 System.out.println("Ingrese los numeros que quiera: ");
			 numeros[i] = Integer.valueOf(sc.nextLine());
			 
		 }
		 for(int j = 0; j < numeros.length; j++) {
			 System.out.println(numeros.length-numeros[j]+1);
		 }*/
		
		/*int[] numeros = {1,3,5,7,9};
		
		public static void desplazarDerecha(int[] numbers) {
			int ultimo = numbers[numbers.length-1];
			
			for (int i = numbers.length-2;i>=0;i--) {
				numbers[i+1]=numbers[i];
			}
			numbers[0]=ultimo;
		}
		public static void desplazarIzquierda(int[] numbers) {
			int primero = numbers[numbers.length-1];
			
			for (int i = numbers.length-2;i>=0;i--) {
				numbers[i+1]=numbers[i];
			}
			numbers[numbers.length-1]=primero;
		}
	
	}
}*/
		/*Realizar un programa que solicite por teclado números entre 1 y 1000
		hasta que el usuario informe de que ya no desea introducir más
		números. Después debe mostrar:
		•
		•
		•
		•
		La media de los números leídos.
		Cuántos números terminan en cada uno de los dígitos (0 .. 9).
		El dígito en el que más números terminan.
		En qué dígitos no ha terminado ningún número.
		Pista: crear un vector de contadores de 10 elementos.
		*/
		

		
		/*do {
			System.out.println("Introduce numeros y 0 para terminar");
			numeros= Integer.valueOf(sc.nextLine());
			
		}while(num!=0 || num>0 && num<1000);
			*/
	
		
		
		
	
	


